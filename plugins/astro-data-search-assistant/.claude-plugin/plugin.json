{
  "name": "astro-data-search-assistant",
  "displayName": "Astro Data Search Assistant",
  "description": "Plan and run a rigorous, read-only search across public astrophysics archives (NASA, ESA, CDS, community) for an astronomy data-discovery question, and return a deterministically-ordered list of candidate datasets with provenance, ordering rationale, and caveats. Human-in-the-loop — never downloads, writes download scripts, or endorses a \"best\" dataset.",
  "version": "0.2.0",
  "author": {
    "name": "Pushwitha Krishnappa",
    "email": "pk0055@uah.edu"
  },
  "license": "Apache-2.0",
  "userConfig": {
    "astroquery_mcp_url": {
      "type": "string",
      "title": "Astroquery MCP server URL",
      "description": "HTTP endpoint of the astroquery-mcp server to use (e.g. https://coming-gray-slug.fastmcp.app/mcp). Provided at install rather than hard-coded, so you can point the plugin at your own deployment.",
      "sensitive": false,
      "required": true
    },
    "astroquery_mcp_key": {
      "type": "string",
      "title": "Astroquery MCP token",
      "description": "FastMCP bearer token (fmcp_...) for the astroquery-mcp server, which provides all of this plugin's search tools (astroquery_execute over 14 astronomy archives + ADS literature). The server rejects unauthenticated requests (HTTP 401), so this token is required — without it no search works.",
      "sensitive": true,
      "required": true
    }
  }
}
